<?php
/*$server = "localhost";
$user = "root";
$password = "";
$db = "iidt";
$con = mysqli_connect($server, $user, $password, $db);

if (!$con ){

    die("unable to connect");

} 
*/

$server = "localhost";
$user = "root";
$password = "";
$db = "iidt";
$con = mysqli_connect($server, $user, $password, $db);

if(!$con){
    die("unable to connect");
}
